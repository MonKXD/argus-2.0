/**
 * R-TST-07: "The scoring golden test (AI_SPEC 5.4) changes only together
 * with a SCORING_VERSION bump." Matches the demo fixtures' existing
 * "1.0.0" literal (src/demo/), which predates this real constant.
 */
export const SCORING_VERSION = "1.0.0";
