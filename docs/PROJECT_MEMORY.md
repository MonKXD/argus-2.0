# ARGUS AI — Project Memory

Version-controlled memory for this project: decisions, conventions, gotchas and session notes. It is separate from Claude Code's own auto-memory and is loaded into every session through `CLAUDE.md`, so keep it lean (about 150 lines; move older session notes to `docs/memory-archive/`).

How to update: add a decision when you choose between real options; add a gotcha when something cost time; add a session note at the end of each working session. Never store secrets, real company data or document content here.

Last updated: 2026-09-22 (T-2.12)

## 1. Snapshot

- Product: evidence-first startup due diligence platform. Every claim is `VERIFIED`, `AI_ANALYSIS`, `ASSUMPTION` or `MISSING`. Trust is enforced in code.
- Stage: documentation complete; implementation begins at Phase 0 (see TRACKER "Current focus").
- Stack: Next.js App Router, TypeScript strict, Tailwind, shadcn/ui, Firebase (Auth, Firestore, Storage), Anthropic API, Zod, Vitest, Playwright, pnpm.

Pinned versions (Node 22, pnpm 10.33.0):

| Package | Version | | Package | Version |
|---|---|---|---|---|
| next | 16.3.5 | | @playwright/test | 1.63.0 |
| react / react-dom | 19.2.8 | | @testing-library/react | 16.3.3 |
| typescript | 5.9.3 | | @testing-library/jest-dom | 7.0.1 |
| tailwindcss | 4.3.3 | | @testing-library/user-event | 14.6.7 |
| zod | 4.6.5 | | @vitejs/plugin-react | 6.1.1 |
| pino / pino-pretty | 10.3.1 / 13.1.3 | | jsdom | 30.1.0 |
| @radix-ui/react-slot | 1.3.3 | | msw | 2.15.0 |
| class-variance-authority | 0.7.1 | | eslint / eslint-config-next | 9.39.5 / 16.3.5 |
| clsx / tailwind-merge | 2.1.1 / 3.7.0 | | prettier | 3.9.8 |
| lucide-react | 1.47.0 | | firebase-tools | 15.30.2 |
| tw-animate-css | 1.4.0 | | @types/node | 26.6.2 |

TypeScript is pinned to 5.9.3, not the newly-published 7.0.2 (the Go-based
`tsgo` compiler) — too new for this ecosystem's tooling to have caught up to.

Model IDs in use (defaults are in `.env.example`; wired up at T-2.07):

| Role | Model ID | Since |
|---|---|---|
| ANALYSIS | claude-sonnet-5 | T-2.07 |
| SYNTHESIS | claude-sonnet-5 | T-2.07 |
| FAST | claude-haiku-4-5-20251001 | T-2.07 |

Eval log (one row per `pnpm eval` run that matters):

| Date | Prompt / scoring version | Result | Notes |
|---|---|---|---|
| | | | |

Run baseline (fill in at T-2.18): median cost per run, median duration, downgrade rate.

## 2. Decisions

| ID | Date | Decision | Reason | Status |
|---|---|---|---|---|
| D-001 | 2026-09-21 | Product is ARGUS AI. Output is considerations and a score, never a buy or pass verdict. | Trust and legal posture | Accepted |
| D-002 | 2026-09-21 | Stack: Next.js App Router, TypeScript strict, Tailwind, shadcn/ui, pnpm. | Fits solo build and the Claude Code workflow | Accepted |
| D-003 | 2026-09-21 | Backend: Firebase Auth, Firestore, Storage. Clients are read-only; all writes go through route handlers with the Admin SDK. | Simple, secure by default | Accepted |
| D-004 | 2026-09-21 | Claims backed only by `PROVIDED` evidence are labelled "Sourced". "Verified" is used only when at least one `INDEPENDENT` source supports the claim. Data status stays `VERIFIED`; the label is derived. | A founder statement is not independent confirmation (PRD OQ-5) | Accepted, revisit after beta |
| D-005 | 2026-09-21 | The engine is a UI-independent library behind `EvidenceStore`, built CLI-first in Phase 2. | De-risk the core value before building the app | Accepted |
| D-006 | 2026-09-21 | Structured output via forced tool call, Zod validation, one repair attempt. Model IDs from env only. | Reliability and easy model changes | Accepted |
| D-007 | 2026-09-21 | Scoring is deterministic and versioned. The model scores rubric criteria; code computes dimension score, confidence and overall. | Explainable, comparable, testable | Accepted |
| D-008 | 2026-09-21 | Insufficient-evidence gate (coverage below 0.5 gives no headline score). An open critical flag caps the score at 60. | Avoid false precision; policy assumption, revisit with beta data | Accepted |
| D-009 | 2026-09-21 | Orchestration: inline step machine first; queued mode in Phase 6 only if the host cannot safely run the longest runs. | Avoid infrastructure until needed | Accepted |
| D-010 | 2026-09-21 | Run progress is delivered by Firestore listeners on the run document, not by holding an HTTP stream. | Resumable, decoupled from the request | Accepted |
| D-011 | 2026-09-21 | Export order: Markdown and JSON, then print-optimised page for browser PDF, then server PDF later. | Avoid headless-browser complexity early | Accepted |
| D-012 | 2026-09-21 | Design: dark only in v1; "certainty as texture"; Newsreader plus IBM Plex Sans; no brand hue; chromatic colour reserved for the evidence spectrum. | Distinct, subject-specific, avoids generic AI look | Accepted |
| D-013 | 2026-09-21 | Competitor, customer, investor and people names come only from evidence. Independent competitor discovery needs web research. | Prevent fabricated entities | Accepted |
| D-014 | 2026-09-21 | Demo and fixture data is fictional and labelled. | Avoid misrepresentation and real-data exposure | Accepted |
| D-015 | 2026-09-21 | Founder analysis uses professional information only; sensitive-attribute guard in the verifier. | Privacy and fairness | Accepted |
| D-016 | 2026-09-21 | This file is named `PROJECT_MEMORY.md`, not `MEMORY.md`, to avoid confusion with Claude Code's auto-memory file. | Clarity | Accepted |
| D-017 | 2026-09-21 | Run modern-web-guidance before any UI, CSS or client-JS work; browser policy in RULES R-UI-13. | Current best practice, fewer dependencies | Accepted |
| D-018 | 2026-09-21 | Client Firestore reads limited to `analyses` and `runs`; everything else is read server-side. | Minimal exposure and rules cost | Accepted |
| D-019 | 2026-09-21 | Phase 1 (design) and Phase 2 (engine) are independent and may be interleaved. | Visible progress plus risk reduction | Accepted |
| D-020 | 2026-09-21 | T-1.01 contrast check (WCAG AA, computed from the real token hex values): every text and evidence-spectrum colour clears 4.5:1 on both Ink and Slate (worst case: Haze/Missing at 4.56:1 on Slate). Haze fails AA text contrast on Surface-3 (4.15:1) but clears the 3:1 non-text minimum there; Surface-3 is reserved for non-text use (the ScoreGauge track), so this is a guardrail, not a violation — automated in `tests/unit/contrast.test.ts`. | DESIGN section 3.2 asks for an automated check recorded here | Accepted |
| D-021 | 2026-09-21 | T-1.01 tabular-numeral check: inspected the self-hosted IBM Plex Sans (400) file directly (fontTools) — digits 0-9 are all 600 units wide by default, i.e. genuinely tabular, not gated behind an optional `tnum` OpenType feature (which this build doesn't declare). `font-variant-numeric: tabular-nums` is safe to use as-is; no fallback to Plex Mono for numeric columns needed. | Verified by parsing the actual font file rather than assuming (R-PRC-08) | Accepted |
| D-022 | 2026-09-21 | T-1.02 overlay primitives: Dialog and Sheet are hand-built on native `<dialog>` (DESIGN section 12); Select, Tabs, Popover, Tooltip and Toast use Radix. Checked via modern-web-guidance rather than assumed: CSS anchor positioning isn't reliably native anywhere yet, and Customizable `<select>` (`appearance: base-select`) is Chrome/Edge-only — both fail R-UI-13's cross-browser bar, so "native lacks the behaviour" applies to those five. | R-UI-01/R-PRC-08: checked the actual guides instead of assuming | Accepted |
| D-023 | 2026-09-21 | Table uses `@tanstack/react-table` pinned to 8.21.3, not the npm "latest" dist-tag (9.2.4). The 9.x line is stale (published 2022, predates the actively-maintained 8.x line's 2026 releases) and has a completely different API (`useTable`/`createTableHook` instead of the standard `useReactTable`/`flexRender`/`getCoreRowModel`) — a real footgun in the registry, not a deliberate current release. | Verified via `npm view ... time.created`/`time.modified` rather than trusting the dist-tag (R-PRC-08) | Accepted |
| D-024 | 2026-09-22 | T-1.03 pulled `src/lib/schema/enums.ts` (ClaimStatus, Reliability, + the rest of SCHEMA.md section 2's enums) forward from Phase 2, verbatim from the doc. The full Evidence/Claim/Report/Analysis/Run schemas still land at T-2.01; this is only the enum vocabulary the UI components needed now, per R-COD-03 (types are z.infer of schemas, not hand-written duplicates). | Avoids a UI-only shadow type Phase 2 would have to reconcile | Accepted |
| D-025 | 2026-09-22 | ScoreGauge/DimensionRadar's confidence buckets (Low/Medium/High) come from AI_SPEC section 5.3 ("below 0.35 Low, 0.35 to 0.65 Medium, above 0.65 High"), in `src/lib/confidence.ts`, shared by both charts rather than invented per-component. Charts live in `src/components/charts/` per TRD's repo map, not `src/components/argus/` (that's for StatusBadge/EvidenceBar-style domain components). | Single source for a threshold the engine will also use | Accepted |
| D-026 | 2026-09-22 | T-1.08 pulled the rest of SCHEMA.md sections 2-5 forward (superseding D-024's "lands at T-2.01"): `src/lib/schema/{ids,evidence,claims,report,analysis,run}.ts`, verbatim from the doc. Needed because "demo dataset typed with schemas" (T-1.08's own spec) requires the real `Analysis`/`Report` shapes, and R-COD-03 forbids a hand-written duplicate. T-2.01 is now mainly the remaining TQ-2 decision (JSON Schema conversion for the model's tool call) plus section 6 (Comparison/Activity/Signal/Export), not a from-scratch build. | No new design decisions were needed — SCHEMA.md already fully specifies these; only R-PRC-07's "changing schemas" gate applies to an actual design change, not implementing what's already written down | Accepted |
| D-027 | 2026-09-22 | `Iso` uses `z.iso.datetime()`, and URL fields use `z.url()`, not `z.string().datetime()` / `z.string().url()` as SCHEMA.md originally showed. Both are `@deprecated` in the pinned Zod 4.6.5's own type definitions (same runtime behaviour, current spelling). Fixed in both the doc and `src/lib/schema/`. | Checked the installed package's types rather than copying the doc's snippet verbatim (R-PRC-08/R-COD-09) | Accepted |
| D-028 | 2026-09-22 | Demo/fixture IDs (`src/demo/ids.ts`) are `${prefix}_${n.toString().padStart(26, "0")}` — digits only, deterministic, satisfies `idOf()`'s regex. The real prefixed-ULID generator (R-COD-04) is deferred to Phase 2 (T-2.01/T-2.02): demo data is static and never needs a fresh unique ID, so building the real generator now would be unused scaffolding. | Avoids speculative infrastructure ahead of the task that actually needs it | Accepted |
| D-029 | 2026-09-22 | T-1.09 `AppShell`: sidebar collapse state is in-memory only (no localStorage persistence) — DESIGN only requires it to be collapsible, not for the choice to survive a reload, and persisting it would need a read-on-mount effect that either mismatches the SSR'd markup or trips the "no setState synchronously in an effect" lint rule for a requirement that doesn't exist. `/app/compare`, `/app/watchlist`, `/app/settings` nav links point at routes that don't exist until Phase 4/5; left as ordinary `<Link>`s (ordinary click-to-fetch, no `prefetch={false}` special-casing) rather than working around Next's harmless prefetch 404s. | Don't build for a requirement nobody asked for; don't hide an honest "not built yet" behind a flag | Accepted |
| D-030 | 2026-09-22 | T-1.10 `CommandPalette` uses native `<dialog>` (like Dialog/Sheet), not Radix or `cmdk`. DESIGN section 12's "use Radix where native lacks the behaviour (menus, combobox, command palette)" is about the *wired* palette (T-5.10's real fuzzy search/listbox), not this skeleton, which is a static link list — no combobox behaviour exists yet to need Radix for. Revisit at T-5.10. | No new dependency for behaviour this task doesn't have | Accepted |
| D-031 | 2026-09-22 | T-1.11 (landing) needs real `ClaimInline` and evidence-rail components per DESIGN 5.6 ("built from the real ClaimInline and evidence rail components... not a screenshot"), but the tracker schedules the full versions as their own Phase 4 tasks (T-4.08 evidence rail/sheet, T-4.09 ClaimRow/ClaimInline + status filter). Asked the user; chose to pull forward a minimal version now rather than mock the hero or resequence the tracker. Built `src/components/argus/claim-inline.tsx` (ClaimInline only, not ClaimRow — no report-page gutter exists yet to put ClaimRow in) and `evidence-rail.tsx` (`EvidenceRailContent`: quote/source/locator/reliability/based-on, given already-resolved data — no responsive column-vs-sheet chrome, no live Firestore lookups). T-1.12 adds the interactive open/close a rail actually needs; T-4.08/T-4.09 extend these with report-page features rather than rebuilding. | User-directed scope call on a genuine doc-sequencing conflict (R-PRC-06) | Accepted |
| D-032 | 2026-09-22 | T-1.12 `/sample`: the evidence rail is a docked 360px column on `lg+`, and the *same* content reflows inline below the sections on narrow screens via plain CSS grid placement — not DESIGN's literal "bottom sheet on narrow screens". A real modal sheet needs a `matchMedia` viewport check so it doesn't call `showModal()` (and trap focus) on desktop too; that complexity is T-4.08's to justify once there's a full report page, not this M-sized preview's. | Avoid a fragile matchMedia hack for a preview task; defer the real responsive-overlay work to where it's actually warranted | Accepted |
| D-033 | 2026-09-22 | T-1.13 dashboard: no "Recent activity" panel, even though DESIGN's mockup shows one — there's no Activity schema/data (deliberately not pulled forward in T-1.08/D-026; it's T-5.08's job). `DemoBanner` is placed on the dashboard page itself, not baked into `AppShell`, since once Phase 3 lands real (non-demo) analyses most `/app` pages should stop showing it — putting it in the shell now would just need undoing later. Market intelligence is sector-mix only (not the mockup's score-distribution/common-risks too): those need `evidenceStats`/`Flag` data this demo set only has for Loopwell, and faking them from three analyses with no such data would be worse than a narrower, honest panel. | Don't show a module or a stat with nothing real behind it | Accepted |
| D-034 | 2026-09-22 | T-1.15 `TableSkeleton`/`KpiStripSkeleton` have no live trigger anywhere in Phase 1 — every page still reads synchronous demo-data imports; real async Firestore/API fetching starts in Phase 3. Built them now anyway (sized to the real `AnalysesTable`/`KpiStrip` layout, demoed only in `/dev/ui`) so Phase 3 wires a loading state up to existing, spec-matching components instead of inventing one under deadline. Retrofitted every currently-reachable ad-hoc empty-state `<p>` (`WatchlistPanel`, `InProgressPanel`, the analyses-list no-match message, plus a new defensive "no analyses yet" guard on `DashboardPage` that APP_FLOW 5.2 requires but the 4-row demo fixture never triggers) to the real `EmptyState` component. | Ready components beat inventing loading/empty markup ad hoc once Phase 3 needs it; ahead-of-need only where the shape is already fully specified (DESIGN section 6) | Accepted |
| D-035 | 2026-09-22 | T-1.16 added `@axe-core/playwright` as a dev dependency (asked the user first per R-COD-08/R-PRC-07) — no native alternative audits WCAG rules, and R-TST-06 requires axe coverage on critical flows. `tests/e2e/a11y.spec.ts` scans landing, `/sample`, `/app`, `/app/analyses` and `/dev/ui` at 1440/768/390 (wcag2a/aa, wcag21a/aa tags), plus keyboard-operability and reduced-motion checks. It's not wired into CI: T-0.09 scoped the GitHub Actions workflow to lint/typecheck/unit-test/build only, matching every other Playwright spec already in the repo — this doesn't change that scope. | Verify Phase 1's accessibility claims with a tool, not by eye | Accepted |
| D-036 | 2026-09-22 | T-1.17 found and fixed a real WCAG AA failure: `Button`'s `destructive` variant used `text-white` on the `ember` token background (3.06:1, needs 4.5:1) — inherited shadcn boilerplate, never caught before because nothing in the app had actually rendered that variant until this task's gallery demo. Changed to `text-ink` (6.17:1; `ink` and `ember` are both existing tokens, so this stays within R-UI-02, not a new colour). Same pass also fixed a gallery-only bug: the demo `Select` had no accessible name (axe `button-name`, critical) — added a visible `<Label>` wired via `aria-labelledby`, the same pattern DESIGN already requires for `Input`. Both regression-tested: `tests/unit/contrast.test.ts` (ink-on-ember ratio) and `tests/unit/button.test.tsx` (destructive variant's className). | Fix real accessibility bugs the gallery surfaced, with tests that keep them fixed | Accepted |
| D-037 | 2026-09-22 | T-2.01 resolves TQ-2: structured-output tool schemas (`submit_facts`, `submit_dimension_analysis`, etc., R-AI-08) are generated with Zod 4.6.5's own `z.toJSONSchema()`, not the `zod-to-json-schema` npm package — that package exists to cover Zod 3, which had no built-in converter; the pinned Zod version already ships one. `src/lib/schema/to-json-schema.ts` wraps it as `toToolInputSchema()`, stripping the `$schema` meta key Anthropic's tool `input_schema` doesn't expect. Verified against a discriminated union (`Claim`) directly: converts cleanly to `oneOf` branches with `const` status literals and `additionalProperties: false` per branch, ready to use as-is. Also pulled forward SCHEMA.md section 6's remaining Firestore-model schemas (`Comparison`, `Activity`, `Signal`, `Export`), verbatim, completing `src/lib/schema/`. | No new dependency needed — verified via the installed package's own type/runtime, not assumed (R-PRC-08) | Accepted |
| D-038 | 2026-09-22 | T-2.02 `EvidenceStore`/`FileStore` (`src/lib/analysis/store/`): implemented exactly the TRD 5.1 interface shape, no extra accessor methods (e.g. no `getReport`/`getRun`) — a real need for one will come from an actual step task, not this one (same "don't build ahead of need" call as D-028/D-029). `FileStore` writes one JSON file/directory per Firestore-model collection (SCHEMA.md section 6's own subcollection shape: `sources.json`, `evidence.json`, `facts.json`, `runs/<id>.json`, `reports/<id>.json`, `reports/<id>/dimensions/<key>.json`) under a caller-supplied `rootDir`; `put*` methods upsert by id so a re-run step overwrites rather than duplicates (R-ARC-03). Also resolved D-028's deferred item: `newId(prefix)` in `src/lib/schema/ids.ts` is a real, hand-rolled ULID generator (Crockford Base32, the same alphabet `idOf()`'s regex already used) — no dependency, verified against 100k generated ids for uniqueness and format before writing tests. Not monotonic within the same millisecond (the base ULID spec's random suffix, not the monotonic variant); nothing here needs stricter ordering than millisecond resolution. | Implement exactly the documented interface shape; small well-specified algorithms don't need a dependency | Accepted |
| D-039 | 2026-09-22 | T-2.03 resolves TQ-1: PDF extraction uses `unpdf` (asked the user first with real npm-registry research per R-COD-08 — pinned exact version, not `^`). It wraps `pdfjs-dist` with a small runtime-agnostic API (`extractText`, `getDocumentProxy`) and treats the native `@napi-rs/canvas` renderer as a peer dependency, so plain text extraction pulls in no native binary. `src/lib/analysis/ingest/extractor.ts` defines a minimal `Extractor` interface (R-ARC-08) and `pdf-extractor.ts`'s `PdfExtractor` implements it: per-page text with a `{ kind: "page", page }` locator, and a `needsVision` flag (AI_SPEC 3.1: fewer than 20 non-whitespace characters) for pages with no real text layer. Scope call: this task detects and flags vision-fallback pages, it does not render or transcribe them — that needs the LLM client (T-2.07/T-2.08), which doesn't exist yet, so `@napi-rs/canvas` isn't installed until a real caller needs `renderPageAsImage`. `maxPages` (mirrors `MAX_PDF_PAGES`) is a constructor param, not read from `env` directly, keeping the class a pure testable unit (same pattern as `FileStore`'s `rootDir`). Tests feed real bytes through the real library: a minimal valid PDF is built at test time with programmatically-computed xref byte offsets (not hand-counted, not a checked-in binary fixture). | Purpose-built library over raw `pdfjs-dist` or `pdf-parse` (which hard-depends on the native canvas even for text-only use); real bytes over a mocked library in tests | Accepted |
| D-040 | 2026-09-22 | T-2.04 added `mammoth` (DOCX) and `exceljs` (XLSX), asked first with real npm-registry research; CSV got a hand-rolled RFC 4180 parser instead of a library (same call as `newId`'s ULID encoder, D-028) since its quoting rules are small and well-specified, and TXT/MD share one `TextExtractor` (no library — blank-line paragraph splitting works for both without a Markdown parser, since Markdown's own block syntax already sits between blank lines). Locators follow SCHEMA's existing kinds: DOCX → one `paragraph` page per `<p>`/`<h*>`/`<li>` block (mammoth's HTML output, split with a scoped regex rather than a DOM parser, safe here since the HTML is mammoth's own predictable markup, not arbitrary input); XLSX → one `sheet` page per worksheet; CSV → one `sheet` page for the whole file (no sub-file boundary exists). exceljs's own `.d.ts` declares a local `Buffer extends ArrayBuffer {}` that shadows Node's real `Buffer` and doesn't structurally match it — worked around with a cast through `Parameters<typeof workbook.xlsx.load>[0]`, not `any` (R-COD-01). All four new extractors are tested against real files: a shared `tests/helpers/build-minimal-zip.ts` hand-builds valid, uncompressed ZIP packages (DOCX/XLSX are both OOXML zips) using `node:zlib`'s built-in `crc32()`, extending T-2.03's "real bytes, not a mock" pattern. | Small well-specified formats (CSV, TXT/MD) don't need a dependency; DOCX/XLSX structure genuinely needs a real parser | Accepted |
| D-041 | 2026-09-22 | T-2.05 (`src/lib/analysis/ingest/safe-fetch.ts`, R-SEC-06): no new dependency — the whole SSRF guard (IPv4/IPv6 CIDR checks including IPv4-mapped/NAT64 embedded-address unwrapping, robots.txt parsing, HTML→text, link extraction) is hand-rolled, verified with real numeric test vectors before writing code (R-PRC-08) rather than trusted by construction. Guards against DNS-rebinding TOCTOU specifically: resolves and validates the hostname itself, then pins the actual request's `lookup` option to that exact validated address, rather than trusting a second, separate DNS resolution inside the HTTP client. Testability required a narrow, explicitly-named escape hatch (`unsafeAllowPrivateNetworksForTests`) so integration tests can hit a local `127.0.0.1` server for the "everything except the network-target check" behaviour (redirects, content-type/size caps, robots.txt, same-origin crawl), while a separate, unconditional test suite (`safe-fetch-ip-ranges.test.ts`) exercises the real production range-check directly with no bypass. `WebsiteExtractor` doesn't implement the shared `Extractor` interface (its input is a URL, not a `Buffer`) and isn't `ResearchProvider` either (T-2.14's separate, independent web-research job) — it's specifically "ingest the founder's own submitted website." | A security guard this consequential needs to be verified against real vectors and tested against real requests, not asserted by inspection; the bypass flag stays out of any production code path by being an explicitly-named, single-purpose test seam | Accepted |
| D-042 | 2026-09-22 | T-2.06 found a genuine doc/schema gap: AI_SPEC 3.1 requires INGEST to raise a `SOURCE_INTEGRITY` `Flag` on a detected injection pattern, but `Flag.detectedBy` (`claims.ts`, verbatim from SCHEMA.md) was `z.enum(["CONSISTENCY", "DIMENSION", "VERIFIER"])` — no value for the ingest step itself. Asked the user (R-PRC-07's schema-change gate); added `"INGEST"` to the enum in both files. `src/lib/analysis/ingest/build-evidence.ts`'s `buildEvidence()` is the actual T-2.06 deliverable: turns `ExtractedPage[]` into `Evidence[]`, further chunking (`chunk-text.ts`) any unit still over 2,000 chars on a paragraph/sentence/word boundary with a 150-char overlap, sanitising (`sanitize.ts`: strips control/zero-width chars, light whitespace normalisation only — R-AI-03 needs the stored text to still exact-match a verbatim quote), hashing (`node:crypto` sha256), assigning `Reliability` by `SourceType` per AI_SPEC 3.1's table, and detecting (not removing) instruction-like text (`injection-detection.ts`). Deliberately stops short of constructing the actual `Flag`/`RunWarning` records — those need a real `analysisId`/`runId`-scoped INGEST step that doesn't exist yet (same "don't build ahead of need" call as D-028/D-038); `buildEvidence()` instead returns `injectionMatches` (evidence id + matched pattern name) for that future step to act on. | A real doc gap gets a real schema fix (asked first), not a workaround; the function that would need Flag-construction context it doesn't have yet stays scoped to what it can actually do | Accepted |

| D-043 | 2026-09-22 | T-2.07 (`src/lib/ai/`): added `@anthropic-ai/sdk` 0.127.0 exact-pinned (asked first, real npm-registry research — MIT, not deprecated, published days earlier). The user first asked about switching the whole project from Anthropic to a free provider (Gemini or similar); clarified the reasoning was wanting genuinely no-cost usage, then laid out that this pipeline's own design (up to 4 concurrent dimension calls per run, an 8-fixture eval harness) would blow through any free tier's rate limits immediately, so a switch wouldn't actually make the real workload free — plus unit/CI tests never call a live model regardless of provider (R-TST-02), and the only cost that occurs in practice (manual `pnpm analyze`/`pnpm eval`) is a few cents per run at Sonnet 5 pricing. User chose to keep Anthropic as documented. `models.ts` (`modelForRole`), `pricing.ts` (real per-model USD/MTok table verified today against platform.claude.com/docs/en/about-claude/pricing, keyed by model family with the release-date suffix stripped), `errors.ts` (`LlmSchemaError`, `LlmResponseError`, `PricingNotFoundError`), `client.ts` (lazy-singleton SDK client from env), `llm.ts` (`AnthropicLLM implements LLM`: forces the single named tool via `tool_choice`, temperature fixed at 0, one repair round-trip sending Zod's validation errors back as an `is_error` tool_result on failure, sums usage/cost across both calls), `budget.ts` (a standalone `Budget` token accumulator against `RUN_TOKEN_BUDGET` — record/remaining/exceeded only; deciding what to *do* when exceeded is the step runner's job, T-3.08, which doesn't exist yet). `AnthropicLLM` takes its Anthropic client and model-ID map via constructor injection rather than reading `env` itself, so it's fully unit-testable (same "constructor param, not env" pattern as `FileStore`/`PdfExtractor`, D-038/D-039) — a `createAnthropicLlm()` factory wires the real env-backed instance for a future caller. Retries-with-jitter on 429/5xx (AI_SPEC section 8) come from the SDK's own built-in retry policy (verified in its source, not assumed) rather than a hand-rolled loop. Prompt-caching (TRD 5.3): `cachePrefix` becomes its own `cache_control: { type: "ephemeral" }` text block ahead of the uncached `user` block. 22 new tests (MSW-mocked `POST /v1/messages`, R-TST-02); the SDK's own browser-credential-leak guard fires under vitest's jsdom environment, so tests construct the client directly with `dangerouslyAllowBrowser: true` — never set in the production `client.ts`. | Real workload economics settle the provider question better than the free-vs-paid framing did; dependency-inject the SDK client so the class stays a pure testable unit; trust the SDK's verified built-in retry over reimplementing it | Accepted |

| D-044 | 2026-09-22 | T-2.08 found a genuine formula gap: `Fact.confidence` (0-1) has no formula anywhere in the docs, even though AI_SPEC section 5 is explicit the model never outputs it. Asked the user (R-PRC-07's scoring-change gate); chose to reuse AI_SPEC 5.2's existing `quality_c` reliability-weight table (INDEPENDENT 1.0, FIRST_PARTY 0.7, PROVIDED 0.5) at the fact's own (strongest-quote) reliability, rather than inventing a new number — documented in AI_SPEC 3.2. `src/lib/analysis/scoring/reliability-weight.ts` extracts that table now (`reliabilityWeight`, `strongestReliability`) since T-2.10's dimension `quality_c` needs the identical table later — one source, not a second copy to keep in sync. `src/lib/analysis/verify/citation.ts` implements V1 (citation validity): exact contiguous-substring match after normalising case/whitespace/quotes/dashes/markdown, falling back to a windowed word-level fuzzy match (hand-rolled Levenshtein similarity, ≥0.92 threshold) — both paths return a real slice of the original evidence text, never the model's own copy, and both were verified against real test vectors (including a timing check on worst-case-sized inputs, ~160ms) before being trusted, per R-PRC-08 and R-TST-01's 90%-coverage bar for `verify/`. `src/lib/analysis/steps/extract-facts.ts`'s `extractFacts()` is the actual T-2.08 deliverable: one `submit_facts` LLM call per source (AI_SPEC 3.2's "batch by source"), V1 on every quote (dropping invalid ones, dropping a fact with none left and recording `CITATION_INVALID`), reliability/confidence assignment, and duplicate-fact merging (same key+value+period; distinct values stay separate for CONSISTENCY). Not wired to `EvidenceStore`/`StepContext` — same scoping as T-2.06's `buildEvidence()`, since the real idempotent step runner is T-3.08's job. No coverage tool is installed to verify the 90% bar numerically; the `verify/`/`scoring/` test suites were written to exercise every branch by inspection instead. | A real formula gap gets a real answer (asked first) that reuses an existing documented number instead of inventing a new one; extract the shared reliability-weight table before a second task needs to copy it; a fuzzy-match algorithm this consequential gets verified against real vectors, not assumed | Accepted |

| D-045 | 2026-09-22 | T-2.09 found the same gap as D-044, one level up: `Claim.confidence` (0-1) also has no formula anywhere, and it needs values for four statuses. Asked the user; extended D-044's resolution rather than inventing unrelated numbers — VERIFIED reuses the reliability-weight table (strongest cited evidence), AI_ANALYSIS reuses 5.2's own 0.25 constant, and the two genuinely new values were the ones put to the user: ASSUMPTION = 0.1 (weaker than an inference — an unconfirmed premise) and MISSING = 0 (asserts nothing to have confidence in). `src/lib/analysis/scoring/claim-confidence.ts`. | Extend an already-agreed principle rather than re-litigate it, but still ask before fixing genuinely new numbers | Accepted |
| D-046 | 2026-09-22 | T-2.09 found a second gap: AI_SPEC section 6's V4 (status integrity) and V7 (instruction leakage) rows name no `WarningCode` for their claim drops, unlike V1/V2/V3/V6 which each name one. V7 is out of this task's scope (AI_SPEC 3.5 runs only V1-V4 and V6 during ANALYZE; V7 is VERIFY's, T-2.13). Asked the user about V4; added `"INVALID_REFERENCE"` to `WarningCode` in `enums.ts` and `SCHEMA.md`, and named it in AI_SPEC 6's V4 row, rather than misusing `SCHEMA_REPAIR` (which is specifically the LLM client's own repair-call mechanism, section 8 — a different concept that would have mislabelled these warnings). | A wrong-but-existing code is worse than a small, asked-for enum addition — same class of gap as D-042 | Accepted |
| D-047 | 2026-09-22 | T-2.09 (`src/lib/analysis/`): TRACKER splits ANALYZE work into T-2.09 ("rubrics, clamps and per-dimension validation") and T-2.10 ("scoring module... golden test"), but `DimensionAnalysis.score`/`.confidence` (AI_SPEC 5.2's formula) are required, non-nullable schema fields a single dimension's own data is enough to compute — producing anything less from T-2.09 would be an invalid, incomplete object. Built 5.2's formula now (`scoring/dimension-score.ts`), verified directly against AI_SPEC 5.4's worked Founder numbers (score 65, confidence 0.69) as a real golden-test unit test. T-2.10 is now specifically 5.3's *cross-dimension* combination (weighted overall score, coverage gate, critical-flag cap) — a separate computation needing all 8 `DimensionAnalysis` results at once, which 5.4's golden test also covers end-to-end once T-2.10 lands. `src/lib/schema/rubrics.ts` (the 8×5-6 criterion table, verbatim from AI_SPEC 4); `prompts/{dimension-analysis,fact-block}.ts` (7.3/7.7, `CandidateClaim` with a model-chosen `localId` the same pattern as T-2.08's `CandidateFact`, remapped to real `clm_` ids by code); `verify/{numeric-grounding,entity-grounding,status-integrity,sensitive-attributes}.ts` (V2-V4, V6 — V1 already existed); `scoring/{verified-support,criterion-clamps,claim-confidence,version}.ts`; `steps/{evidence-selection,analyze-dimension,analyze}.ts`. V2's `derivation` check does not evaluate `formula` as an expression (no syntax is defined anywhere and executing arbitrary model-written expressions is an unneeded risk) — it verifies the `inputs` cite real facts with matching values and that the claim's own asserted number is within 1% of `derivation.result`. V6's lexicon deliberately favours precision over recall (multi-word phrases, person-context prefixes) since a false positive destructively strips a legitimate claim — verified against a real fixture set of violations and ordinary business phrases ("Asian markets", "family office", "a single round", "engaged users" all correctly pass) before being trusted. V4's referential check treats "known" as every id that existed in the response up front, not a transitive fixed-point over which claims survive every other check — a claim citing another claim that itself later gets dropped for an unrelated reason is not chased through a cascade; a documented scope boundary, not a bug. `steps/evidence-selection.ts`'s per-call budget (60,000 characters) is an undocumented, chosen default in the same class as D-008 — TRD 5.3 requires *a* budget and simple lexical ranking but never a number. Not wired to `EvidenceStore`/`StepContext`; same scoping as T-2.06/T-2.08. `analyze.ts` bounds concurrency via a caller-supplied parameter (env.ANALYZE_CONCURRENCY, not read directly) and catches a per-dimension failure into a `failed` list rather than aborting the other 7 (TRD 5.2: one dimension failing marks the run PARTIAL, not the whole run failed). | A required, non-nullable schema field forces the scope boundary rather than the tracker's own task split; a destructive verifier check needs precision-favouring patterns verified against real business language, not just real violations | Accepted |

| D-048 | 2026-09-22 | T-2.10 found and fixed a real numerical bug while golden-testing 5.3's overall confidence: the naive `Math.round(value * 100) / 100` T-2.09's `dimension-score.ts` already used rounds an exact-decimal `.xx5` value the wrong way whenever IEEE-754 float accumulation happens to land it a few units of the last place *under* the true boundary — AI_SPEC 5.4's own worked overall confidence (0.138+0.088+0.084+0.04+0.045+0.04, exactly 0.435 in decimal) computes as `0.43499999999999994` in JS and rounds down to 0.43, not the doc's 0.44. Extracted a shared `src/lib/analysis/scoring/round.ts` (`round2`, an epsilon-nudged round, verified against both this case and T-2.09's own AI_ANALYSIS-only golden case, which had the identical bug and an accordingly-wrong test assertion of 0.57 instead of the correct 0.58 — fixed retroactively, not left in place now that the cause was found) and pointed both `dimension-score.ts` and `overall.ts` at it. Known, accepted limitation: a value engineered to sit within the epsilon (1e-9) of a `.xx5` boundary is indistinguishable from drift and still rounds up either way; every real input here is a sum of at-most-4-decimal-place weights/scores, so that case doesn't arise in practice. `src/lib/analysis/scoring/overall.ts`'s `computeOverall()` is the actual T-2.10 deliverable: AI_SPEC 5.3's cross-dimension combination (weighted score, the <0.5-coverage `INSUFFICIENT_EVIDENCE` gate, D-008's open-critical-flag cap at 60, recorded whenever a critical flag is open even if it wasn't the binding constraint), golden-tested against 5.4's exact end-to-end numbers (score 63, confidence 0.44, coverage 0.78). `src/lib/analysis/config.ts` (SCHEMA.md section 7 names this exact file and location) holds `SCORING_VERSION`, the 8×3-stage-profile weight table and the `Stage`→`StageProfile` mapping — consolidated from T-2.09's `scoring/version.ts`, which SCHEMA.md's own text already pointed at `config.ts` for. | A doc's own worked example is itself a correctness check on the implementation, not just documentation to copy from — a golden test that reproduces the wrong number by a false "floating point is what it is" excuse is worse than no golden test; fix the root cause everywhere it appears, including in already-shipped code | Accepted |

| D-049 | 2026-09-22 | T-2.11 found a scope ambiguity: AI_SPEC 3.4 says `UNVERIFIABLE_CLAIM` (a VERIFIED, superlative-language claim supported only by `PROVIDED` evidence) is "handled during ANALYZE", but 3.5's detailed ANALYZE step spec never describes it, and T-2.09 (which built the rest of ANALYZE) shipped without it. Asked the user; chose to build it fresh in T-2.11 rather than retrofit T-2.09's already-shipped, already-tested `analyze-dimension.ts`. `src/lib/analysis/verify/superlatives.ts`'s `containsSuperlative()` uses a small pattern list (first-to-market, "only X", "no competitors", "#1", "best-in-class", "market leader", "unmatched"/"unparalleled") verified against real true-positive competitive-superiority claims and false-positive mundane phrasing (e.g. "first quarter") via a scratchpad script before being trusted, same rigor as T-2.09's V6 lexicon (D-047); the `#1` pattern needed `/#\s?1\b/` without a leading `\b`, since `\b` never matches immediately before a non-word character like `#`. `src/lib/analysis/steps/unverifiable-claims.ts`'s `detectUnverifiableClaims()` is the deliverable: fixed `MEDIUM` severity, `detectedBy: "DIMENSION"` per AI_SPEC 3.4. `src/lib/analysis/flags.ts` extracts `materialitySeverity(factKey)` (AI_SPEC 3.4's revenue/funding/team-size/customer-count → HIGH, else MEDIUM) since T-2.11's own `INCONSISTENCY` flags need it and a future flag-producing step will too. CONSISTENCY itself (`steps/consistency.ts`, `steps/consistency-candidates.ts`, `prompts/consistency.ts`) follows the doc's deterministic-pass-then-model-adjudication design: `findConsistencyCandidates()` groups facts by `key` only, not `(key, period)` — grouping by period too would make the model's own `DIFFERENT_PERIOD` verdict unreachable, since a same-period pair is the only case that verdict could ever correctly describe; this was reasoned from the model's own verdict space rather than asked, as a contained implementation detail rather than a schema/policy change. Numeric candidates reuse `withinTolerance()` from T-2.09's `numeric-grounding.ts` (V2) at 5%, inverted, rather than a second copy. The model (FAST role) adjudicates each candidate pair as `CONFLICT`/`DIFFERENT_PERIOD`/`ROUNDING`; only `CONFLICT` produces an `INCONSISTENCY` flag and sets `conflictsWith` on both facts (a `Map<string, Set<string>>` accumulator, deduplicated). No candidates found → no LLM call at all (`usage: undefined`), matching T-2.08/T-2.09's zero-waste pattern. Not wired to `EvidenceStore`/`StepContext`; same scoping as T-2.06/T-2.08/T-2.09/T-2.10. | A real doc gap (spec says "handled during ANALYZE" but the detailed spec omits it, and the shipped code doesn't have it) gets asked and built, not silently skipped or silently retrofitted into already-shipped code; a verdict space with an unreachable branch is a design bug worth reasoning through before coding, not after | Accepted |

| D-050 | 2026-09-22 | T-2.12 (SYNTHESIZE, AI_SPEC 3.7): the model gets no raw evidence in this step's prompt (only overall, dimension claims, flags, canonical facts, per 7.6) — so a narrative `VERIFIED` claim can't carry model-authored quotes to V1-validate the way ANALYZE's can. `CandidateNarrativeClaim`'s `VERIFIED` variant instead carries `restatesId` (a real `clm_` or `fct_` id); code resolves it via `resolveRestatement()` and copies that record's exact quotes and confidence, never trusting the model to retype them. `restatesId` can point at a dimension claim (must be `VERIFIED`, else dropped `CITATION_INVALID`) or a canonical fact directly (facts are inherently evidence-grounded — extract-facts.ts only ever creates one from already-V1-validated quotes — so no separate status check is needed for a fact target). `src/lib/analysis/prompts/synthesis.ts` (`buildSynthesisPrompt`, rendering `<dimension>`/`<claim>`/`<flag>` blocks plus the existing `<fact>` renderer) and `src/lib/analysis/steps/synthesize.ts` (`runSynthesis`) are the deliverable: V6, V3, V4 (`AI_ANALYSIS` only) and V2 run on every narrative claim (no V1, per above), mirroring T-2.09's `finalizeClaim`; checklist items resolve `linkedClaimIds` against both pre-existing dimension-claim ids and this response's own newly-authored narrative-claim ids (via the same localId-to-real-id map), dropping an item if none resolve. Scope call, not asked (a contained implementation detail, same class as T-2.11's period-grouping call, D-049): `AI_ANALYSIS.basedOn` and checklist `dimension`/`null-criteria`/`open-flag` provenance are taken at the doc's literal wording — "existing claim or fact ids" — so `basedOn` resolves only against ids that existed before this call, not other narrative claims authored in the same response; narrative claims carry no `derivation` field (no new computed numbers are permitted, so there is nothing for one to justify). | The model has no evidence text to cite from in this step, so quote validation has to be a lookup against already-validated records instead of a fresh V1 pass; a checklist item with no resolvable linked claim doesn't get to exist per the doc's own "each item links to the claims that caused it" | Accepted |

Pending decisions (resolve at the named task, then add a D-entry):

| Topic | Resolve in |
|---|---|
| Web research provider (TQ-5) | T-2.14 |
| Hosting and verified request limits (TQ-3, OQ-7) | T-3.14 |
| Queue technology (TQ-4) | T-6.03 |

## 3. Conventions

- IDs: prefixed ULIDs (`ana_`, `run_`, `clm_` and so on). Enums are UPPER_SNAKE. Fields are camelCase.
- Files: components PascalCase, everything else kebab-case. Named exports.
- Commits: conventional commits with the task ID, for example `feat(engine): add scoring module (T-2.10)`.
- Fixtures: fictional companies only, under `evals/fixtures/<slug>/`.
- Docs are updated in the same change as the code they describe.

## 4. Gotchas and lessons

- `@` imports in `CLAUDE.md` are ignored inside code spans and code blocks. Imported files load in full at session start, so keep `RULES.md` and this file lean.
- Firestore rules are not filters. Any client list query must include an `ownerId` filter. The app lists on the server.
- Firestore documents are limited to 1 MiB. Dimension analyses live in a subcollection for this reason.
- Storage rules can be satisfied by a spoofed content type. Always re-validate uploads by content (magic bytes) on the server. Some browsers send `application/octet-stream` for `.md`; the client sets `contentType` explicitly.
- In inline orchestration mode the handler must ignore client disconnects; do not tie the pipeline to the request abort signal.
- The CSS `overlay` property has limited support; exits are unanimated in some browsers, which is acceptable.
- Numeric grounding exempts bare integers of 10 or below (known limitation, AI_SPEC V2).
- Tabular numerals in IBM Plex Sans: confirmed, see D-021. `font-variant-numeric: tabular-nums` works as-is.
- Next.js 16 generates `LayoutProps`/`PageProps` globals into `.next/types`; plain `tsc --noEmit` fails on them until those types exist. `pnpm typecheck` runs `next typegen` first.
- `next dev` auto-appends a `<!-- BEGIN:nextjs-agent-rules -->` block to `CLAUDE.md` pointing at `node_modules/next/dist/docs/` for this Next.js version's breaking changes vs. training data. It re-adds itself if removed; commit it rather than fight it.
- Prettier's markdown formatter escapes bare `*` as emphasis (e.g. `FR-*` becomes `FR-_`), corrupting the spec docs' glob-style IDs. Root and `docs/` markdown are excluded from Prettier (`.prettierignore`).
- The `shadcn` CLI's `init`/`add` need `ui.shadcn.com`, which this sandbox's network policy blocks outright (403 at the CONNECT layer). `raw.githubusercontent.com` and `api.github.com` are reachable. T-0.04's Button primitive was hand-written (cva + @radix-ui/react-slot + cn), matching shadcn/ui's own convention, instead of run through the CLI. Same workaround applies to T-1.02's remaining primitives unless the policy changes.
- This sandbox pre-installs Chromium at `/opt/pw-browsers/chromium` outside Playwright's own managed cache. `playwright.config.ts` stays portable (normal browser resolution by default); set `PLAYWRIGHT_CHROMIUM_PATH=/opt/pw-browsers/chromium` as an env var (not committed) to point the `chromium` project at it in this sandbox.
- A Tailwind `display` utility (e.g. `flex`) applied unconditionally to a native `<dialog>`/`[popover]` element overrides the UA stylesheet's `:not([open]) { display: none }` — author origin always wins over UA origin, specificity tie or not — so the element stays visible and interactive after "closing". Scope any `display` utility on top-layer elements to `open:` (e.g. `hidden open:flex`), same as the opacity/transform transition classes (T-1.02, Sheet).
- jsdom doesn't implement `HTMLDialogElement.showModal()`/`close()` at all (not even a stub). Native-`<dialog>` components (Dialog, Sheet, CommandPalette) can't be meaningfully unit-tested in Vitest; use Playwright instead (`tests/e2e/overlays.spec.ts`).
- `@testing-library/react`'s auto-cleanup only registers if it detects a *global* `afterEach`; this project's `globals: false` (vitest.config.ts) means it silently never ran, so components rendered by one test leaked into the next and caused ambiguous-query failures once a file had more than one render of the same markup. Fixed by calling `cleanup()` explicitly in `tests/setup.ts`'s own `afterEach`.
- T-0.06: no real Firebase project exists yet. `.firebaserc` points at the demo project `demo-argus-ai` (the Firebase Emulator Suite's offline mode — no GCP project, billing or credentials needed) so `pnpm emulators` works standalone; verified Auth (9099), Firestore (8080) and Storage (9199) all start and answer. **Action needed from the project owner before T-3.01**: create real `dev` and `prod` Firebase projects in the console, add their aliases to `.firebaserc`, and put the Admin SDK service-account values into `.env.local` — an agent session can't do this without your Firebase account.
- `src/lib/env.ts`'s server `env` export originally threw on `typeof window !== "undefined"` to catch a client import early. Dropped it: jsdom (Vitest's default test environment) defines `window` too, so it broke legitimate server-side tests. Next.js already leaves non-`NEXT_PUBLIC_*` vars undefined in the browser bundle, so a client import still fails fast via the normal Zod error, just without the friendlier message.
- `Math.cos`/`Math.sin` can return values that differ in their last bit between Node's V8 build (SSR) and the browser's V8 build (hydration), which shows up as a real React hydration mismatch once those values are embedded verbatim in an SSR'd SVG path `d` string. Only caught via a Playwright console-error check, not any unit test. Fixed at the source: `polarToCartesian` in `src/components/charts/geometry.ts` rounds every coordinate to 2 decimal places, which collapses the sub-visible difference before it reaches the DOM. Applies to every chart built on that shared geometry module (ScoreGauge, DimensionRadar, and future ones).
- `next start` loads the server bundle into memory once; a later `pnpm build` overwriting `.next/` is invisible to an already-running `next start` process, and `playwright.config.ts`'s `reuseExistingServer` will happily hand a fresh test run to that stale server. Symptom looked like real bugs (duplicate accessible names, a random 500) that vanished once the leftover `next-server` process was killed. Check `lsof -i :3000` (or just kill any stray `next-server`) before trusting a Playwright run that follows manual `pnpm start`/`pnpm build` commands in the same session.
- T-1.09 built the app shell in `src/components/shell/`, without checking it against TRD section 4's repo map, which names `src/components/layout/` for exactly this (AppShell, nav, command palette). Caught it 3 commits later while starting T-1.11. Renamed with `git mv` and a project-wide import fix rather than leaving the drift — cheap now (7 files), not later. Lesson: check the repo map for a new top-level component folder before creating one, not after.
- Grid items (like flex items) default to `min-width: auto`, so a wide child — the dashboard's DataTable — grows the whole page instead of scrolling inside its own `overflow-x-auto` wrapper, on narrow viewports. Playwright's `fullPage` screenshot only scrolls vertically, so this doesn't show up as an obvious visual defect in a screenshot the way most bugs do — it silently clips columns off-screen. Caught by checking `document.documentElement.scrollWidth` vs `clientWidth` directly, not by eyeballing a screenshot. Fixed with `min-w-0` on the grid item wrapping the table (T-1.13).
- Same class of bug again at T-1.16, this time on `/dev/ui`: several `flex gap-8`/`flex gap-4` demo rows (ScoreGauge x4, Sparkline x3, the ClaimInline+EvidenceRail row) never wrap, so they overflowed the page at 390px behind fixed `p-16` padding. `document.documentElement.scrollWidth` vs `clientWidth` caught it again where axe and Playwright screenshots didn't (axe doesn't check layout overflow; the screenshot just showed content past the fold). Fixed with `flex-wrap` on those rows and `p-4 sm:p-16` on the page padding. Given the pattern (T-1.13's table, T-1.16's gallery), a scrollWidth/clientWidth check at 390px is now worth running on every new page, not just tables.

## 5. Open questions

See TRACKER "Decisions needed" for the live list (TQ-1 to TQ-5, OQ-1, OQ-8) and PRD section 16 for product questions.

## 6. Session log (newest first)

- 2026-09-22: T-2.12 done (synthesis step — narrative and checklist, FR-ENG-07). The model gets no raw evidence in this prompt (only overall, dimension claims, flags, facts, AI_SPEC 7.6), so a narrative `VERIFIED` claim can't carry model-authored quotes to V1-validate — `CandidateNarrativeClaim`'s `VERIFIED` variant carries `restatesId` (a real `clm_` or `fct_` id) instead, and code copies that record's exact quotes/confidence rather than trusting a retyped one (D-050). `restatesId` can resolve to a `VERIFIED` dimension claim or directly to a canonical fact (facts are inherently evidence-grounded already, no extra status check needed). `prompts/synthesis.ts` (`buildSynthesisPrompt`, rendering `<dimension>`/`<claim>`/`<flag>` blocks) and `steps/synthesize.ts` (`runSynthesis`) run V6/V3/V4/V2 on every narrative claim (no V1, per above), mirroring T-2.09's `finalizeClaim`; checklist items resolve `linkedClaimIds` against both pre-existing dimension claims and this response's own newly-authored narrative claims, dropping an item with nothing resolvable. `AI_ANALYSIS.basedOn` deliberately resolves only against ids that existed before this call (the doc's literal "existing claim or fact ids"), not other narrative claims in the same response — a documented scope boundary, not asked (same class as T-2.11's period-grouping call, D-049). Not wired to `EvidenceStore`/`StepContext` — same scoping as T-2.06 through T-2.11. 16 new tests. `pnpm check` (522 unit tests), `pnpm build` green. Next: T-2.13 (verifier V1 to V7 with unit tests). Older entries: `docs/memory-archive/2026-09-session-log.md`.
